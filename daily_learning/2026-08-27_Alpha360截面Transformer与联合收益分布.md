# 2026-08-27 Alpha360 截面 Transformer 与联合收益分布

## 用户确认的方案

优先实现本模型，TRA 后排。Mac 用于开发和小测试，台式机 RTX 3060 12GB
用于正式训练。不同时跑两个 GPU 训练任务。

输入为历史 CSI1000 成分股的 Alpha360：每只股票 60 个交易日、6 个量价通道。
不能把股票列表的顺序当成位置；时间位置编码只加在 60 天维度上。
用户提出并采纳：建立股票代码词典，每只股票对应随机初始化后冻结的 16 维身份向量。
身份向量不是行业信息，冻结也不等于不会过拟合；后续可用 `--stock-embedding-width 0` 对照。

## 网络与损失

1. 每个时间点的 6 维特征线性映射至 64 维，加可学习的时间位置编码。
2. 共享时序 Transformer：2 层、4 heads、FFN=256、dropout=0.1。
3. Attention pooling 得到每只股票的 64 维历史表示。
4. 拼接冻结的 16 维股票身份向量，再经 80→64 映射。
5. 截面 Transformer：2 层、4 heads，让同一信号日的全部股票相互注意。
6. 输出 9 个数：3 个均值和下三角 Cholesky 矩阵的 6 个元素。

可训练参数约 21 万。模型虽然小，60 天时序和约 1000 只股票的注意力仍需要计算。
首轮每次处理一个完整日期，累积 4 个日期的梯度后更新一次；不拆散截面。

信号在 T 日收盘后产生；第一版“早盘”=开盘价，“尾盘”=收盘价，
**不是 10:30 和 14:55 的实际可成交价**。

| 三段目标 | 定义 |
|---|---|
| a | ln(C[T+1]/O[T+1]) |
| b | ln(O[T+2]/C[T+1]) |
| c | ln(C[T+2]/O[T+2]) |

模型输出 `(mu_a,mu_b,mu_c)` 和 `L`，并用 `Sigma=L L^T` 构造合法协方差。
L 对角线为 softplus(raw)+0.001；其他三个元素可正可负。
三段联合高斯 NLL 用 Cholesky 求解，不直接求逆，不对秩不足的四目标协方差求逆。

四个输出分别是 `a+b+c`、`b`、`a+b`、`b+c`，对应：

| 文件字段前缀 | 买入→卖出 |
|---|---|
| open1_close2 | 明日开盘→后日收盘 |
| close1_open2 | 明日收盘→后日开盘 |
| open1_open2 | 明日开盘→后日开盘 |
| close1_close2 | 明日收盘→后日收盘，与 Fixed 持仓区间一致 |

四个对数收益满足 y1+y2=y3+y4。普通收益不能直接线性相加。
预测四区间的均值、方差和相关性均由三段联合分布推导。
普通收益期望是 exp(log_mean+log_variance/2)-1，不是 exp(log_mean)-1。

标签不做截面标准化；训练时仅乘固定倍率 100，输出时还原。
编码器 CUDA 使用支持时的 BF16；协方差与 NLL 保持 float32。
AdamW lr=0.0003、weight_decay=0.0001、梯度范数上限 1。
最多 50 epoch；Valid NLL 连续 10 轮不改善早停。
预测方差不是“保证正确的置信度”，需要看概率校准和区间覆盖率。

## 日期、数据与防泄漏

保持 Fixed Fold3 的非训练段，并采用 120 个月名义训练窗：

| 分段 | 开始 | 结束 | 用途 |
|---|---|---|---|
| Train | 2015-04-17 | 2025-04-16 | 梯度训练与输入标准化拟合 |
| Valid | 2025-04-17 | 2025-09-16 | 早停 |
| Selection-valid | 2025-09-17 | 2026-02-16 | 完成后评估，后续选模/集成 |
| Test | 2026-02-17 | 2026-07-17 | 完成后一次评估，不用于逐轮早停 |

Train、Valid、Selection-valid 各最后两个交易日的标签清空，避免 T+2 标签跨段。
训练使用信号日当时的成分股，历史窗口可以包含它进入 CSI1000 之前的已知行情。
不能用当前成分股列表代替历史列表。

**新发现的数据缺口**：本机 csi1000.txt 在 2015 年 4 月至 5 月初只含 3 只股票。
所以导出器要求至少 800 只成分股：不完整的 Train 日期显式排除、写入 manifest；
Valid/Selection-valid/Test 不完整则报错，不悄悄筛掉。名义边界不变，有效训练起点
可能后移，最终以 manifest 的 `excluded_train_dates` 和 `parts` 为准。
800 是数据完整性检查阈值，不是选股数量，也不是要求所有股票都有可用未来标签。

Alpha360 的 360 列按 CLOSE59..0、OPEN59..0、HIGH、LOW、VWAP、VOLUME 排列。
数据导出先读取六种基础行情，再向量化构造相同的 60 天比例特征；
第一块数据会与安装的 Qlib Alpha360DL 全部 360 列及未来价格逐项比对，失败即停止。
这样可以减少大量重复的表达式读取，不能改变因子含义。

输入每列的均值/标准差只在 Train 拟合；非有限值在标准化后填 0。
没有对预测标签做 CSZScoreNorm，也没有用真实标签决定 Attention。

两台电脑的原行情文件哈希不同，所以未直接使用台式机原有行情，
也未覆盖它。已将本机行情打包，放到新实验独立的 provider 目录。
归档 SHA256：`1d5833bbfe31e1e6263c80dfe688336ac8acf5b34342f8f5de2565b602d0dff8`。

## 代码、目录和命令

- 模型：`roll/alpha360_cross_stock.py`
- 数据/训练/评估入口：`script/train_alpha360_cross_stock.py`
- 单元测试：`tests/test_alpha360_cross_stock.py`
- Windows 串行执行器：`script/run_alpha360_cross_stock_windows.ps1`
- Windows 一次性任务启动器：`script/launch_alpha360_cross_stock_windows.ps1`
- Git 分支：`codex/alpha360-cross-stock-nll-260827`

本机实验目录：
`/Users/hmax/qlibAssistant/.qlibAssistant/remote_runs/alpha360_cross_stock_fold3_120m_260827/`

台式机目录：
`E:\qlibAssistant\.qlibAssistant\remote_runs\alpha360_cross_stock_fold3_120m_260827\`

Python：台式机 `E:\Miniconda\envs\qlibass\python.exe`；
本机 `/Users/hmax/miniconda3/envs/qlibAssistant/bin/python`。

在项目根目录，手动执行方式：

```bash
python script/train_alpha360_cross_stock.py export --data /path/to/data --provider /path/to/cn_data
python script/train_alpha360_cross_stock.py train --data /path/to/data --output /path/to/benchmark --benchmark-only
python script/train_alpha360_cross_stock.py train --data /path/to/data --output /path/to/run
```

`--device cpu` 用于本地小测试，默认 `cuda`，不可用时直接报错，不偷偷改成 CPU。
`--segments-json` 接收日期配置文件，不是内联 JSON 字符串。
`script/alpha360_cross_stock_smoke_segments.json` 仅用于代码烟雾测试，不是正式报告。

### 日志与断点

Windows 的 `train.log` 是实时日志，`data/export_status.json` 是导出进度，
`run/status.json` 是训练/评估状态。训练过程中每 25 个日期更新一次状态。

```powershell
Get-Content E:\qlibAssistant\.qlibAssistant\remote_runs\alpha360_cross_stock_fold3_120m_260827\train.log -Tail 30 -Wait
```

运行目录创建 `STOP_AFTER_EPOCH` 文件，会在完成当前 epoch、保存检查点后暂停。
恢复前移走这个标记，再使用相同参数加 `--resume`；不能修改输入、结构或代码后
冒充同一个可恢复实验。每轮检查点包含模型、优化器、随机状态和历史指标。

一次性任务没有每天重跑的触发器；台式机需保持开机和用户登录，不要注销。
Mac 合盖不会终止独立的 Windows 训练任务，但 Mac 不再能实时显示新日志。

### 产物含义

| 产物 | 含义 |
|---|---|
| data/manifest.json | 精确数据段、排除日期、特征定义、样本数、文件校验 |
| benchmark/benchmark.json | 真实截面的训练耗时与估算，不是最终效果 |
| run/configuration.json | 网络、超参、设备、代码与数据哈希 |
| run/stock_ids.json | 冻结身份向量对应的股票代码词典 |
| run/normalizer.npz | 仅 Train 拟合的输入标准化参数 |
| run/best_model.pt | Valid NLL 最优权重及配置 |
| run/last_checkpoint.pt | 可恢复的最近完整 epoch |
| run/epoch_metrics.csv | 每轮 loss、Valid 指标与耗时 |
| run/summary.csv | Valid、Selection-valid、Test 的分布和预测指标 |
| run/*_predictions.csv | 每只股票四种区间的预测均值、方差、概率及真实收益 |
| run/*_daily_metrics.csv | 每日 Rank IC、MAE、Brier、区间覆盖率 |

这是独立实验产物，**暂未注册为 MLflow recorder**。
概率和收益指标还不是严格交易回测。涨跌停、停牌、买入替补、手续费、
event_guard 和主板过滤需要在后续统一执行回测中比较，不能把原始目标收益当成可成交收益。
当前只训练第一组主模型，尚未声称完成随机种子集成或各架构消融。

## TRA 的处理

按用户新优先级，已停止原 TRA 进程 PID 5396，保留原有日志、最佳模型、最近检查点，
另备份 `run/priority_pause_checkpoint_260827.pt`；停止前最后确认完成预训练 epoch 6。
原任务目录为 `tra_fixed_fold3_120m_260826`。
其旧入口尚无自动恢复功能，恢复 TRA 时必须先补齐恢复逻辑，不能直接从头重跑冒充续训。

## 验证与进度记录

- 单元测试已覆盖字段顺序、三段标签、边界 purge、NLL 与 PyTorch 分布一致性、
  股票排列等变性、冻结身份向量、padding mask、反向传播和未来价格不进入特征。
- 已完成 3 股票的极小端到端功能测试；其 IC 不具有经济意义，不作效果汇报。
- 完整截面 CUDA 短测和正式训练状态，见本节后续运行记录。

### 00:51 正式任务已启动

- 12 项单元测试通过；初始实现提交为 `826a0fd`。
- Windows 完整截面烟雾测试通过：1002 只历史股票、20,040 个股票日样本；
  Alpha360 360 列与 Qlib 对照一致；完成一轮训练和所有分段 CSV 导出。
  这是极短功能测试，其收益、IC 不用于推断模型有效性。
- CUDA BF16 可用。该短测从开始训练至全部评估约 3.6 秒，包含冷启动，
  **不能把这个数字当作 120 个月正式训练耗时**。
- 正式一次性任务 `Qlib_Alpha360_CrossStock_Fold3_120m_260827`
  已于 2026-08-27 00:50:50 启动，数据导出主进程 PID 33856。
  截至 00:51 已完成第一块并开始第二块数据；尚未进入正式 epoch。
- 第一块明确排除了 29 个不完整训练日期，有效训练起点为 2015-05-29；
  Valid、Selection-valid、Test 边界没有改变。
- 执行链为：正式数据导出 → 完整截面吞吐短测 → 新建正式模型训练 →
  载入 Valid 最优权重 → 输出 Valid/Selection-valid/Test 指标和预测。
  任一步报错则停止后续步骤，不把失败当作完成。
- 耗时外推会自动写入 `benchmark/benchmark.json`。首个正式 epoch 后，
  用 `run/epoch_metrics.csv` 的 `epoch_seconds` 复核更准确。
- 本次冻结的 Mac 行情日历截止 2026-08-21，满足 Test 最后一个信号日
  2026-07-17 的 T+2 标签要求；不是在声称同步了台式机 8 月 26 日版本。
- Mac 压缩包附带的两个 `calendars/._day*.txt` AppleDouble 元数据文件
  已从独立 Windows 快照删除，否则 Qlib 会误当成行情频率文件。正常行情未改动。
- Mac 本次导出/测试均已退出，没有留下本次 Python 训练进程。

Mac 终端实时查看台式机日志：

```bash
ssh -o HostKeyAlias=192.168.1.7 -o StrictHostKeyChecking=yes -p 22 12600@100.76.140.38 'powershell -NoProfile -Command "Get-Content E:\qlibAssistant\.qlibAssistant\remote_runs\alpha360_cross_stock_fold3_120m_260827\train.log -Tail 20 -Wait"'
```

关闭这个日志查看窗口只会断开查看，不会终止台式机上的一次性训练任务。

### 01:14 训练与自动验收

- 数据导出耗时 366 秒；有效 Train 为 2402 个日期、2,402,042 个股票日，
  其中 2,318,793 行有完整三段标签。Valid/Selection-valid/Test 分别为
  105/100/99 个信号日；前两段最后 2 日不参与标签指标。
- 正式训练 PID 28800。首轮受冷启动与数据读取影响约 290 秒，随后每轮
  约 78～96 秒。估时须区分首轮和稳定吞吐，不能只根据模型参数量猜测。
- 当前 19 项测试通过，包括无未来行情的独立推理输入、自动验收超时和重复执行保护。
- 新增一次性验收任务 `Qlib_Alpha360_Finalize_260827`，轻量检查每 30 秒一次；
  等待期间不占 GPU，不重启训练。其流程是：完成训练 → 独立指标/标签/权重核验 →
  只读取信号日及以前行情重新推理 → 与已保存的最后一个 Test 日预测比对 → 打包。
  状态为 `finalization_status.json`，日志为 `finalize.log`，完成后该进程退出。
- 训练仍由 Valid NLL 选最优 epoch。某一轮 Rank IC 较高但 NLL 不最优，
  不会临时改成按它选权重，更不会用 Test 挑 epoch。

只看紧凑进度（不会启动训练）：

```bash
ssh -o HostKeyAlias=192.168.1.7 -o StrictHostKeyChecking=yes -p 22 12600@100.76.140.38 E:/Miniconda/envs/qlibass/python.exe E:/qlibAssistant/.qlibAssistant/remote_runs/alpha360_cross_stock_fold3_120m_260827/script/status_alpha360_cross_stock.py --root E:/qlibAssistant/.qlibAssistant/remote_runs/alpha360_cross_stock_fold3_120m_260827
```

完成后的独立验收命令（目录必须新建，不能覆盖旧报告）：

```bash
python script/audit_alpha360_cross_stock.py --run /path/to/run --data /path/to/data --output /path/to/audit_new --device cuda
```

验收同时给出完整 CSI1000 和其中沪深主板两种范围，包含四种持仓区间的
Rank IC/ICIR、MAE、Brier、AUC、方向准确率、50/80/95% 区间覆盖率。
常数上涨概率、平均收益和联合高斯基线只用 Train 拟合；主板另拟合自己的基线。
这使“输出概率是否真的比历史上涨率更有用”可以直接检查，而不是只看 IC。

### 以后如何单独推理

完成的 `run` 目录包含权重、词典和标准化参数；推理不需要复制数 GB 的训练数组。
Mac 使用 CPU 即可，不会调用 TRA，也不改变现有 Fixed 每日预测流程：

```bash
/Users/hmax/miniconda3/envs/qlibAssistant/bin/python script/predict_alpha360_cross_stock.py \
  --run /path/to/completed/run \
  --provider ~/.qlib/qlib_data/cn_data \
  --date latest \
  --device cpu \
  --output /path/to/new_prediction_directory
```

- 默认按 `close1_close2_expected_return` 排序；`--rank-horizon` 可选表中其他三个区间。
- 固定输出两个 CSV：`ranking_all.csv`、`ranking_mainboard.csv`，另有 `metadata.json`。
- 代码、名称、板块放前面。名称可通过 `--names-csv` 提供，需含 `instrument,name` 两列；
  未提供时名称留空，不把代码伪装成股票名称。
- `probability_positive` 是模型分布隐含的上涨概率，需结合校准结果理解；
  `return_std` 是普通收益的标准差，不是均值的估计标准误。
- 新出现的股票使用全零未知身份向量并显式标记。输入历史仍是该股票自身过去 60 个交易日。
- `mainboard` 只过滤输出，截面注意力仍使用完整 CSI1000，和训练时一致。
- CSV 尚未应用 event_guard、市场门槛、手续费及可成交约束，不等于实盘买入指令。

### 01:18 Windows 文件锁中断与续训

第 14 轮第 525 个日期附近，发布 `run/status.json` 时 `Path.replace` 报
`PermissionError: [WinError 5]`。这是进度文件原子替换失败，不是 GPU OOM、
NLL 非有限或模型反向传播失败。Windows 上短暂读文件的句柄可能阻止删除/重命名；
具体持有句柄的进程未取证，所以不把某一个监控进程直接定为唯一原因。

处理：

1. 确认训练和旧验收均退出，保留完整错误记录；没有强行忽略训练异常。
2. 备份原源码、原配置及第 13 轮 `checkpoint_before_io_repair.pt`。
3. 原子发布改为有限次数重试，最多约 4.3 秒；持续权限故障仍会报错。
   JSON 和检查点的替换都使用这个函数，训练算法没有变化。
4. 显式 `--resume --resume-io-repair` 仅兼容已记录 SHA256 的原版训练入口；
   数据、网络源码、超参、优化器状态仍严格校验。新旧源码哈希写入 `resume_events`，
   不改写旧权重里的来源信息来伪装成未发生中断。
5. 从完成的第 13 轮恢复模型、优化器和随机状态，重做未完成的第 14 轮。
   续训日志为 `resume_io.log`；旧日志 `train.log` 保留。

21 项单元测试通过，包括临时文件锁会重试、永久错误不会被吞掉；
极小数据上的显式版本修复续训和最终审计也已通过。
本次训练耗时统计包含所有已完成 epoch 与最后一次评估，但不包含中断的半轮、
排查停顿；若要看实际等待多久，应看任务开始和最终结束的墙钟时间。
新的一次性任务分别为 `Qlib_Alpha360_Resume_IO_260827` 和
`Qlib_Alpha360_Finalize_IOResume_260827`，不会自动重复执行。

## 正式完成结果与结论（2026-08-27）

### 完成状态与耗时

- 20 轮后正常早停，最佳模型为第 10 轮，选择依据始终是 Valid 联合 NLL。
- 正式训练完成时间：01:38:44；独立审计、信号日推理重放及打包完成：01:40:48。
- 从 00:50:50 一次性任务启动到打包完成，墙钟约 **50 分钟**，包括导出、
  吞吐测试、训练、文件锁故障处理、续训、评估和验收。
- 仅已完成 epoch 加最终评估共 2265.5 秒，约 **37.8 分钟**；不含故障半轮和处理停顿。
- 稳定 epoch 约 78～80 秒；初次第 1 轮约 290 秒，恢复第 14 轮约 416 秒，
  第 15 轮约 163 秒，第 16 轮起恢复约 80 秒。冷缓存/数据载入会显著影响估时。
- 台式机 RTX 3060，BF16 编码器、FP32 分布运算，210,314 个可训练参数。
- 本次数据源冻结到 2026-08-21；没有偷偷换成另一版行情。
- 台式机训练与验收任务最后退出码均为 0，相关 Python 进程数为 0；
  Mac 的最终 CPU 推理进程也已退出，TRA 保持暂停。

### 测试区间与样本

名义 Test 是 2026-02-17～2026-07-17；实际交易日从 **2026-02-24** 开始，
共 **99 个信号日**。全池有 99,000 行预测、98,847 行完整真实收益；
主板评估有 64,053 个带真实标签的股票日。
指标先逐日计算，再对日期等权平均，不是把所有股票日混成一条时间序列。

这只是一个随机种子、一组网络参数、一个 120m 窗口、Fold3 的首轮模型。
未声称完成 Fold1/Fold2、种子集成或不同结构的全面比较。
历史 Test 区间在此前项目研究中已多次查看；本次训练虽未用它早停，
也不能将它描述为整个研究从未接触过的全新留出集。

### 全部四区间结果：不是只挑好看的汇报

“明日/后日”均相对于信号日 T；开盘/收盘为日线目标价，不是保证成交价。

| 持仓区间 | 全 CSI1000 Rank IC | 全池 Rank ICIR | 主板 Rank IC | 主板 Rank ICIR |
|---|---:|---:|---:|---:|
| 明日开盘 → 后日收盘 | -0.0309 | -0.1935 | -0.0356 | -0.2218 |
| 明日收盘 → 后日开盘 | **0.0967** | **0.5458** | **0.1057** | **0.7588** |
| 明日开盘 → 后日开盘 | -0.0262 | -0.1957 | -0.0302 | -0.2214 |
| 明日收盘 → 后日收盘 | 0.0103 | 0.0693 | 0.0124 | 0.0954 |

主板已排除科创板和创业板；注意力输入仍为完整 CSI1000，与训练保持一致。

| 主板区间 | MAE（收益百分点） | Brier↓ | Train 上涨率基线 Brier↓ | 日均 AUC | 95% 区间覆盖率 |
|---|---:|---:|---:|---:|---:|
| 开盘 → 后日收盘 | 3.19 | 0.2597 | 0.2492 | 0.4975 | 91.72% |
| 收盘 → 后日开盘 | 0.79 | 0.2471 | 0.2452 | 0.5246 | 95.40% |
| 开盘 → 后日开盘 | 2.27 | 0.2544 | 0.2491 | 0.4953 | 93.66% |
| 收盘 → 后日收盘 | 2.30 | 0.2540 | 0.2480 | 0.4963 | 91.95% |

### 这些结果说明什么

1. **原想法已经实现并跑通，但不能说首版整体效果好。**
   与 Fixed 相同的“明日收盘→后日收盘”排序能力较弱；两个从明日开盘进入的
   区间还是负 Rank IC。没有证据据此替换现有 Fixed 每日流程。
2. **隔夜排序是值得保留的研究线索，不是已经验证的可交易收益。**
   主板隔夜 Rank IC=0.1057、Rank ICIR=0.7588，其他区间没有同样表现。
   但隔夜可捕捉的价格变化较小，手续费、集合竞价成交、涨跌停等影响尚未评估。
   不把它写成“95% 胜率”或“比 Fixed 赚钱”。
3. **概率输出尚不可靠。** 四个区间在全池和主板的 Brier 都比仅用 Train
   历史上涨比例的常数基线更差。以主板收盘→收盘为例，预测概率 50%～60%
   这一箱有 59,197 个股票日，平均预测 53.27%，实际上涨只有 44.62%。
   这是概率偏乐观的直接证据，而不是看到模型输出 0.53 就能理解成真实 53% 胜率。
   主板 60%～70% 这一箱仅 15 行，不能据其高样本胜率推导可靠阈值。
4. **方向准确率也要看基线。** 隔夜主板日均准确率为 55.83%，但实际上涨比例
   仅 42.84%，始终预测不涨的同口径准确率约 57.16%；不能单看“超过 50%”。
   涨跌方向判别与横截面收益排序不是同一个任务，所以正 Rank IC 不保证方向概率好。
5. **NLL 的确优于常数联合高斯，但这不等价于选股能力强。**
   Test 联合 NLL 为 6.6188，Train 拟合的常数联合高斯为 6.9997；越低越好。
   模型能通过改善条件波动率/协方差来降低 NLL，即使收益均值和方向预测并未同幅改善。
   因而同时汇报 NLL、排序、概率校准和简单基线是必要的。
6. **区间覆盖不能只看一个 95%。** 隔夜主板 95% 覆盖率约 95.40%，但其
   50% 区间覆盖率约 69.18%，说明中间区间偏宽；收盘→收盘的 95% 覆盖率仅
   91.95%，又存在尾部风险低估。这不是所有概率层级都校准好了。

本轮不再根据 Test 临时调参追指标，不自动扩大模型、重训、恢复 TRA 或上线交易。
若后续继续优化，应先冻结新方案，再用训练/验证段比较；新 Test 证据另行保留。
### 明日收盘到后日开盘：严格交易回测

在统计评估之后，进一步冻结 `close1_open2_expected_return` 分数执行一次独立回测；
没有根据 Test 回测结果修改模型或阈值。信号日为 T，T+1 收盘买入，T+2 开盘卖出。
测试仍是上述 99 个信号日，仅预测 CSI1000 中的沪深主板股票。

严格口径为：初始资金 10 万元、100 股整数手、买卖各 0.0235% 佣金、每笔最低
5 元、按用户当前要求暂不计印花税；用 Tushare `daily` 原始 OHLC 和
`stk_limit` 精确涨跌停价检查可成交性。买入收盘等于涨停价则该槽位留现金，
另外报告向后递补版本；T+2 开盘跌停无法卖出会被单独标记。本期没有一次开盘
跌停无法卖出。CSI300/CSI1000 基准也计算相同的“前一日收盘到次日开盘”毛收益，
而不是拿整日指数收益混比。

下表是不递补版本；“理想毛收益”直接使用标签等权合成，不含成交约束、手数和成本；
“可执行毛收益”加入真实 OHLC、涨跌停和整数手但不扣佣金；净收益再扣实际佣金。

| TopK | 理想毛收益 | 可执行毛收益（0 滑点） | 佣金后净收益（0 滑点） | 每边 5bp 滑点后净收益 | 0 滑点最大回撤 | 净胜率 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | -0.06% | +0.09% | **-4.37%** | -13.29% | -10.50% | 42.42% |
| 3 | +1.38% | +0.65% | **-3.76%** | -12.48% | -7.11% | 44.44% |
| 5 | +5.17% | +4.34% | **-0.85%** | -10.05% | -6.32% | 42.42% |
| 10 | +5.10% | +4.33% | **-5.73%** | -14.25% | -6.92% | 41.41% |

同期 CSI1000 隔夜毛收益为 -7.28%，CSI300 为 -5.40%。Top5 在零滑点、扣佣金后
虽然分别相对它们超额约 +6.93% 和 +4.81%，但绝对收益仍为负；不能把“跑赢下跌基准”
写成赚钱。Top10 因 10 万元分成十份后，8 个候选出现一手也买不起；最低佣金又使
99 天共扣约 9720 元。Top5 佣金约 4920 元，几乎抹掉全部毛收益。

99 天内原始 Top1/3/5/10 分别只有 1/2/3/10 个候选在买入收盘封涨停，远不是收益
主要来自买不到的涨停股。向下递补没有改善：零滑点 Top1 为 -3.75%，Top5 为
-1.15%。真正的主要问题是预测优势集中在全截面排序，Top1 尾部不稳定，且隔夜
价格变化很小、每日往返成本过高。每边 5bp 滑点会再造成约 9 个百分点级别的拖累。

因此，“主板 Rank IC=0.1057”是值得研究的截面统计信号，但本版尚不是可上线策略。
后续只能在 Valid/Selection-valid 上预先设计低换手、置信门槛或持仓衔接方案，再用
新的未观察区间确认；不应继续在这 99 日 Test 上筛出最好看的规则。

严格结果保存在：

`/Users/hmax/qlibAssistant/.qlibAssistant/analysis/alpha360_overnight_backtest_20260827_1020/`

其中 `summary.csv` 是总表，`top*_daily.csv` 是逐日持仓、收益、费用、涨停拦截和
现金比例，`method.json` 固化了所有口径。原始 Tushare OHLC 缓存在
`.qlibAssistant/cache/tushare_daily_ohlc.parquet`，避免此前 `daily_basic` 在
2026-06-19～2026-08-06 的本地断档被误判为不可成交。

### 四个区间全部实盘回测与不确定度过滤

继续对另外三个输出区间执行同一类严格账户回测，并专门验证方差、上涨概率、
风险调整分数和置信下界能否改善 Top1。回测仍限定 CSI1000 沪深主板，初始资金
10万元、100股整数手、买卖各万2.35且最低5元、暂不计印花税、涨停买不到则向后
递补、跌停卖不出则持有到同一成交阶段首个可卖交易日。每边5bp滑点作为主要实盘
口径，零滑点版本也完整保留。

`open1_close2` 会在下一批信号入场时仍持有上一批股票，因此使用两个独立的50%资金
袖套交替入场；其余三个区间在新一批入场前或同一成交阶段先卖旧仓，不重复占用本金。
这避免把同一笔10万元每天重复投入尚未结束的持仓。

#### 未加不确定度过滤的 Test 基线

下表是每边5bp滑点后的净累计收益，共99个Test信号日。所有 TopK 都按期望收益排序，
不可买候选向后递补。

| 持仓区间 | Top1 | Top3 | Top5 | Top10 | Top1交易胜率 |
|---|---:|---:|---:|---:|---:|
| 明日开盘→后日收盘 | -49.73% | -48.25% | -47.60% | -43.45% | 39.58% |
| 明日收盘→后日开盘 | **-12.83%** | -12.67% | **-10.66%** | -14.83% | 35.35% |
| 明日开盘→后日开盘 | -40.66% | -47.26% | -52.32% | -51.35% | 40.21% |
| 明日收盘→后日收盘 | -50.78% | -59.86% | -46.51% | -47.54% | 43.30% |

零滑点但扣佣金的 Top1 依次为 -47.62%、-3.75%、-34.52% 和 -45.91%。因此除
收盘→开盘外，问题并非滑点假设过重；模型原始选股方向已经不合格。同期相同持仓
区间的 CSI1000 毛收益依次为 -11.46%、-7.28%、-16.16%、-13.84%；CSI300 为
+2.07%、-5.40%、-1.76%、+0.07%。四个 Top1 在5bp口径下都没有跑赢对应基准。

#### 不确定度实验设计

一共比较以下预先定义的规则族，Top1为主要对象：

- 按期望收益、上涨概率、`期望收益/标准差` 排序；
- 按 `均值-0.5×标准差`、`均值-1×标准差` 的置信下界排序；
- 只在低方差30%/50%/70%股票中选取正均值候选；
- Top1上涨概率不足50%/52%/53%/55%/60%则整天空仓；
- Top1方差不在当日最低30%/50%/70%则整天空仓；
- 置信下界不为正则整天空仓。

先在 Valid（105日）和 Selection-valid（100日）分别运行。正式稳健选择以两段中
较差的5bp净收益最大为准，并要求两段各至少30个活跃交易日；Test不参与选规则。
同时保留只看Selection-valid挑规则的版本，展示单段选择的过拟合风险。

#### 双区间稳健选择结果

| 区间 | 选中规则 | Valid净收益 | Selection-valid净收益 | Test净收益 | Test胜率 |
|---|---|---:|---:|---:|---:|
| 开盘→收盘 | 上涨概率≥60%的候选替代 | +2.57% | +5.57% | **-20.26%** | 39.58% |
| 收盘→开盘 | 低方差30%中选正均值 | -1.87% | -5.99% | **-25.50%** | 30.53% |
| 开盘→开盘 | 低方差50%中选正均值 | -4.42% | -4.58% | **-47.09%** | 36.36% |
| 收盘→收盘 | 低方差30%中选正均值 | +8.84% | +4.11% | **-14.06%** | 45.45% |

收盘→收盘的低方差规则将Test亏损从 -50.78%降到 -14.06%，开盘→收盘也从
-49.73%降到 -20.26%；方差有一定风险规避信息，但仍没有形成正收益。对唯一具有
稳定正Rank IC的收盘→开盘区间，低方差替代反而把Test从 -12.83%恶化到 -25.50%。

真正“置信不足就空仓”的结果更直接：收盘→开盘的Top1方差低于当日中位数时，
Valid交易15次净收益 -0.13%，Selection-valid交易21次 -6.33%，Test交易28次约
0.00%。后验只看Test会误以为成功，但前两段不支持；概率≥52%在Test仅触发3次且
亏损 -0.86%，≥53%基本不交易。其余区间的最高期望候选几乎从不同时落入低方差
50%，说明“预测涨得最多”与“模型最确定”在本模型中高度冲突。

#### 结果可信度与结论

逐区间、逐日重新用Tushare原始开收盘价计算收益，并与模型保存标签核对：三个分段
全部区间相关系数均高于0.999，差异超过50bp的记录为0。Test四区间最大单条差异
分别约5.46、28.72、5.43、13.55bp；因此不存在把T+1/T+2错位导致巨亏的问题。

本轮结论是：**不确定度可以减少部分风险，但不能把首个Alpha360模型变成可交易
模型。** 模型的概率校准本来就不优于常数基线；高Rank IC只存在于收盘→开盘的全
截面排序，无法稳定延伸到Top1尾部或日期择时。单个Selection-valid曾出现+50%～
+66%的幸运策略，Test随即跌到-40%左右，正是不能按单段累计收益挑策略的例子。

完整可复现结果位于：

`/Users/hmax/qlibAssistant/.qlibAssistant/analysis/alpha360_four_horizon_uncertainty_20260827_1420/`

- `test_baseline_four_horizons.csv`：四区间Top1/3/5/10基线；
- `valid_uncertainty_grid.csv`、`selection_valid_uncertainty_grid.csv`：Test前规则矩阵；
- `valid_selection_robust_chosen_rules.csv`：双区间稳健选择；
- `test_robust_uncertainty_rules.csv`：冻结规则后的Test结果；
- `robust_selection_test_comparison.csv`：最适合直接阅读的对比表；
- `test_uncertainty_grid_diagnostic_only.csv`：仅供诊断，禁止据此反选规则；
- `label_alignment.csv`：原始OHLC与模型标签的日期/数值核对。

### 验收证据与跨设备推理

- 21 项单元测试通过。
- 小样本“连续两轮”与“训练一轮、恢复后再一轮”的模型权重逐项完全一致。
- 正式全部数组哈希、360 列因子顺序、标签边界、预测覆盖、四区间代数关系、
  概率公式、冻结身份向量、源码版本、模型随附词典和标准化参数检查通过。
- 三个分段首日均做检查点重放；最后 Test 日 2026-07-17 再通过一个独立入口，
  只读取当日及之前行情重新推理 1000 只股票，与已存 Test 预测一致。
- 同步包 SHA256 与台式机一致，ZIP CRC 检查无误，三个预测 CSV 的哈希逐项一致。
- Mac CPU 上同一天 1000 股票推理成功。CPU FP32 与 GPU BF16 不是逐位相同：
  四种区间分数的 Spearman 相关均超过 0.9998，最大收益差小于 0.0001
  （小于 1 个基点）；四区间 Top10 重合数依次为 8/10、10/10、9/10、10/10。
  因此不能承诺不同精度下，极接近的分数排序完全不变。

### 在哪里看、怎么用

已同步并解压到：

`/Users/hmax/qlibAssistant/.qlibAssistant/remote_runs/alpha360_cross_stock_fold3_120m_260827/completed/`

先看 `audit/report.md`，再看 `audit/metrics_compact.csv` 和
`audit/probability_calibration.csv`；逐股票真实收益与预测在
`run/test_predictions.csv`，每轮训练记录在 `run/epoch_metrics.csv`。
最佳权重为 `run/best_model.pt`。这些是独立实验目录，不是 MLflow recorder。
压缩包约 43MB，解压后约 101MB；多 GB 原始训练数组保留在台式机，未重复复制到 Mac。

本轮实际模型的 Mac 推理命令（项目根目录运行）：

```bash
/Users/hmax/miniconda3/envs/qlibAssistant/bin/python script/predict_alpha360_cross_stock.py \
  --run /Users/hmax/qlibAssistant/.qlibAssistant/remote_runs/alpha360_cross_stock_fold3_120m_260827/completed/run \
  --provider /Users/hmax/.qlib/qlib_data/cn_data \
  --date latest \
  --device cpu \
  --output ".qlibAssistant/analysis/alpha360_experimental_$(date +%Y%m%d_%H%M%S)"
```

这个命令只产生实验预测，不会下单，也不替换日常三路预测。若要完全沿用正式
报告的计算精度，在台式机相同环境使用 `--device cuda`；Mac 不用 MPS 训练。
