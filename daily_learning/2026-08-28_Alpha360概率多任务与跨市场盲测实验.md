# 2026-08-28 Alpha360 概率多任务与跨市场盲测实验

## 1. 本轮目的

本轮不是继续微调旧 Fixed，而是严格比较六种 A 股概率预测结构和一个 A+美股
跨市场扩展。所有模型使用相同 A 股日期切分、Alpha360 定义和训练制度；模型和
ensemble 只能查看 `valid`、`selection_valid`，最终 `test` 在联合选择冻结后才允许
首次物化。

四个预测区间均以信号日 T 为基准：

| horizon | 交易区间 |
|---|---|
| `open1_close2` | T+1 开盘买入，T+2 收盘卖出 |
| `close1_open2` | T+1 收盘买入，T+2 开盘卖出 |
| `open1_open2` | T+1 开盘买入，T+2 开盘卖出 |
| `close1_close2` | T+1 收盘买入，T+2 收盘卖出，和旧 Fixed 标签区间一致 |

目标是概率分布，不只是一个排序分数。每个 horizon 输出对数收益均值、方差、
普通收益期望、普通收益标准差和上涨概率；最终仍需用严格可成交回测判断是否可用。

## 2. 对 8 月 27 日首版说明的正式纠正

8 月 27 日文档记录的是最早版本，后续讨论已经改变训练制度。本轮正式实验以这里
为准：

- 股票身份 embedding 是 **64 维、随机初始化、可学习**，不是冻结的 16 维。
- 使用真实日期 batch：不同日期股票数先 padding，再用 mask 屏蔽；张量形状为
  `[B, N_max, 360]`。不是一次只传一个日期。
- `date_batch_size=4`，每个真实 batch 立即做一次 optimizer step。
- **不使用梯度累积，不使用梯度裁剪。** 源码和任务参数都没有相关操作。
- 完整训练 **50 epoch，不早停**。Valid 只为每个 horizon 保留 RankIC 最高的 epoch。
- AdamW，初始学习率 `3e-4`，weight decay `1e-4`；前 3 轮线性 warmup，之后
  cosine annealing 到 `1e-6`。
- 台式机用 CUDA BF16 编码器；概率分布关键运算保持 FP32。
- RTX 3060 实测 E6：batch 2/4/8 每轮纯训练估时约 93.7/79.6/178.8 秒，
  batch 4 最快。batch 越大不保证越快，因为截面 Attention 的时间/显存随股票数
  近似按 `B*N^2` 增长。

## 3. 固定数据切分与职责

| split | 日期 | 唯一用途 |
|---|---|---|
| Train | 2015-04-17～2025-04-16 | 拟合输入标准化、更新网络参数 |
| Valid | 2025-04-17～2025-09-16 | 每模型、每 horizon 选择最佳 epoch |
| Selection-valid | 2025-09-17～2026-02-16 | 选单模型/Top2/全部等权 mixture，并选择交易规则 |
| Test | 2026-02-17～2026-07-17 | 冻结模型与交易规则后的最终描述性评估 |

有效 Train 共 2402 个交易日、约 240 万股票日；Valid/Selection-valid/Test 分别
约 103/100/99 个可评价信号日。模型权重不能由 Selection-valid 或 Test 反向更新；
交易规则也不能由 Test 挑选。

## 4. E0～E6 模型矩阵

### 4.1 E0：三段联合高斯

E0 先预测三个基础对数收益 `(a,b,c)` 的三维联合高斯：3 个均值和一个 3×3
协方差的 Cholesky 六参数。四种交易收益由线性组合推导，因此能保留区间之间的
相关性。损失是三维联合 Gaussian NLL。

E0 已完成 50 轮，墙钟约 49.8 分钟。各 horizon 的 Valid RankIC 最佳 checkpoint：

| horizon | epoch | Valid RankIC |
|---|---:|---:|
| `open1_close2` | 1 | 0.04825 |
| `close1_open2` | 20 | 0.11467 |
| `open1_open2` | 28 | 0.06044 |
| `close1_close2` | 28 | 0.05915 |

以上只是 Valid 选权重结果，不是 Test，更不是严格收益。E0 的 Selection-valid 已
按 horizon 分别重放，100,000 行；四个 checkpoint 不强制来自同一 epoch。

### 4.2 E1：共享编码器、四个独立高斯头

一个 Alpha360 时间编码器和一个 CSI1000 截面 Transformer，共享股票状态；四个
独立 MLP head 分别输出一个 horizon 的均值和标准差。Loss 是四个独立 Gaussian
NLL 的 mask 均值。

### 4.3 E2～E5：四个完全独立模型

每个交易区间单独训练完整的时间编码器、身份 embedding、截面 Transformer 和
Gaussian head。这样可以判断共享 backbone 的多任务干扰是否伤害某些 horizon，
代价是训练四次。

### 4.4 E6：A 股与美股跨市场模型

E6 输入同一天的 CSI1000 Alpha360 和严格 as-of 对齐的 501 只美股 Alpha360：

- A 股和美股使用 **不同参数** 的时间编码器、时间位置编码、attention pooling、
  股票 embedding 和独立标准化器；不会强行共享行情尺度。
- 每个市场先编码成 64 维股票 token，再拼接 16 维市场 embedding，映射到共同
  64 维空间。
- A 股与美股 token 一起经过 2 层 cross-market Transformer；只对 A 股输出四个
  Gaussian head。
- 美股只能使用中国信号时点之前已经收盘的数据；`same-calendar-day US close` 禁止。

E6 store 含 501 只美股、2706 个 A 股信号日、1,312,437 个美股 Alpha360 行；
输入指纹为：

```text
faad6d003534bd034a26b3ebb57fd62378c07a998e9b433551b6fd303954608a
```

限制：美股池是当前 S&P 500 近似，存在 survivorship bias；所以 E6 能检验“海外
信息是否有增量”，不能据此声称已得到无偏的历史美股 universe。

## 5. 真正的 padded date batch

同一个 batch 内不同日期股票数不同是常见问题，解决方式是 padding 和 mask：

```text
features:  [B, N_max, 360]
stock_ids: [B, N_max]
mask:      [B, N_max]
```

Attention 的 padding key 被 mask，Loss 也只统计 `mask=True` 且标签有限的股票。
因此不会把 padding 当成股票，也不需要用梯度累积伪装大 batch。

E6 有两套独立 mask：`a_mask` 与 `us_mask`。A/US 股票数量可同时变化，padding 后
再进入 cross-market Attention。

## 6. 盲测锁箱与断点恢复

本轮把“看见一个 CSV 就跳过”改成“哈希认证完整才跳过”：

1. 候选必须有配置、数据 manifest、Selection 预测、Selection summary、每个
   checkpoint 的路径与 SHA-256，以及 `test_read=false`。
2. Selection 输出先写到 staging 目录，CSV 和 manifest 通过一次目录 rename
   共同发布；失败不会留下看似完整的半成品。
3. E0～E5 完成后，基础流水线停在 `selection_ready`，明确保持
   `test_access_authorized=false`、`test_read=false`。
4. E6 完成后，按预注册 cross-market protocol 对 E0～E6 的 Selection-valid
   结果统一比较，冻结入选候选及 checkpoint 哈希。
5. 只有联合 manifest 写入后，才物化入选候选的 Test；每个候选和最终 aggregate
   都必须有 completion audit。
6. Test 不能用于改变模型、组合或阈值。后续新设计必须使用未来 lockbox。

原先 6 个严格 `xfail` 契约缺口已全部修复；当前 Alpha360/E6/锁箱相关测试
116 项通过。

## 7. Selection ensemble 规则

每个 horizon 独立比较：

- Eligible：Selection-valid RankIC 有限且大于 0；
- 候选组合：最佳单模型、最佳两模型等权 mixture、所有 eligible 模型等权 mixture；
- 主指标：Selection-valid RankIC；
- tie-break：RankICIR 更高、Gaussian NLL 更低、组件更少。

多个高斯做等权 mixture 时：

```text
mixture_mean = mean(mu_i)
mixture_variance = mean(var_i + mu_i^2) - mixture_mean^2
P(r>0) = mean(P_i(r>0))
```

普通收益期望是各组件 log-normal 期望的平均，不把 mixture 简化成一个高斯后再
错误计算。

## 8. 严格回测口径

模型完成后同时报告 `mainboard`（剔除科创板、创业板）和 `all`：

- 初始资金 10 万元；100 股整数手；
- 买卖佣金各万 2.35，单笔最低 5 元；按用户要求暂不计印花税；
- 同时报 0 bps 与每边 5 bps 滑点；
- 使用 Tushare 原始 OHLC 和逐日精确涨跌停价；
- 涨停不可买时分别支持顺延候选与留现金口径；
- 跌停卖不出则继续持仓，并在以后每个可观测的开盘/收盘事件按时序重试；
- 日线成交量为0时视为停牌，不允许虚拟成交；
- `open1_close2` 因持仓跨两个交易日，用两个 50% 时间 sleeve，避免资金重复使用；
- Selection-valid 选择不确定度规则，Test 只执行冻结规则；
- 输出净累计、胜率、最大回撤、交易次数、空仓日、涨跌停阻塞、CSI300/CSI1000
  同期收益和超额收益。

## 9. 运行目录与查看方式

Mac 分支：

```text
codex/alpha360-cross-stock-nll-260827
```

台式机 E0～E5：

```text
E:\qlibAssistant\.qlibAssistant\remote_runs\alpha360_probabilistic_matrix_260828
```

台式机 E6 与最终联合结果：

```text
E:\qlibAssistant\.qlibAssistant\remote_runs\alpha360_e6_full_260828
```

实时查看基础队列：

```bash
ssh -o HostKeyAlias=192.168.1.7 -o StrictHostKeyChecking=yes -p 22 \
  12600@100.76.140.38 \
  'powershell -NoProfile -Command "Get-Content -Raw E:\qlibAssistant\.qlibAssistant\remote_runs\alpha360_probabilistic_matrix_260828\pipeline_status.json"'
```

查看 E6/联合队列：

```bash
python script/finalize_alpha360_e0_e6_local.py --status-only
```

完成后执行一次本地严格回测与报告（不是后台常驻进程）：

```bash
python script/finalize_alpha360_e0_e6_local.py
```

本地最终目录默认是：

```text
.qlibAssistant/analysis/alpha360_e0_e6_260828/
├── selection_manifest.json
├── selection_valid_ensemble_predictions.csv
├── test_summary.csv
├── test_predictions.csv
├── epoch_metrics/
│   ├── E0_joint_three_leg.csv
│   ├── E1_shared_four_head.csv
│   ├── E2_single_open1_close2.csv
│   ├── E3_single_close1_open2.csv
│   ├── E4_single_open1_open2.csv
│   ├── E5_single_close1_close2.csv
│   └── E6_a_us_four_head.csv
├── strict_backtest_mainboard_fallback/
├── strict_backtest_all_fallback/
├── strict_backtest_mainboard_leave_cash/
├── strict_backtest_all_leave_cash/
├── training_curves/
│   ├── training_curve_summary.csv
│   ├── training_curves.png
│   └── training_curves.md
├── report_fallback/
└── report_leave_cash/
    ├── concise_summary.csv
    ├── model_selection.csv
    ├── prediction_metrics_comparison.png
    ├── strategy_equity_curves.png
    └── method_and_findings.md
```

这个 finalizer 每次只检查一次；远程未完成时退出码为 3，不会留一个“幽灵 Python”
持续耗电。真正的等待由 Windows Scheduled Task 完成。

## 10. 当前进度（2026-08-28 03:05）

- E0：50/50 完成，Selection-valid 已物化，Test 锁定；
- E1：50/50 完成，四路 Valid 最佳 checkpoint 和 Selection-valid 候选均已物化；
  `validate-candidate` 已重新校验协议、配置、数据、预测和 checkpoint SHA-256；
- E2：已自动接棒，单 horizon 首轮约61秒；E3～E5继续排队；
- E6：Scheduled Task 已启动，状态 `waiting_for_e0_e5_selection`，不占 GPU；
- 联合 Test、严格回测和报告：尚未执行，不能提前汇报效果。

E1 共享四头模型从 Valid checkpoint 迁移到 Selection-valid 的结果：

| Horizon | Valid最佳epoch | Valid最佳Rank IC | Selection-valid Rank IC | Selection-valid Rank ICIR |
|---|---:|---:|---:|---:|
| `open1_close2` | 46 | 0.0441 | 0.0338 | 0.2070 |
| `close1_open2` | 33 | 0.1142 | 0.1077 | 0.4853 |
| `open1_open2` | 1 | 0.0646 | 0.0510 | 0.2818 |
| `close1_close2` | 46 | 0.0603 | 0.0179 | 0.1114 |

其中 `close1_open2`（T+1收盘买、T+2开盘卖）在两个开发时间段都明显最强；
但这仍是模型选择前的开发结果，不是冻结Test或严格账户回测结论。

关键提交：

```text
d1c79ea  harden alpha360 resumable test pipeline
680fa92  queue blind e0-e6 cross-market evaluation
2535ad4  add one-shot e0-e6 strict report finalizer
```

后续必须完整汇报全部四个 horizon，不能只挑某个表现最好区间；也必须把概率指标、
严格收益、基准、可成交约束和风险放在一起解释。

## 11. 自动收尾与补充报告指标

Mac 已安装透明的 LaunchAgent：

```text
~/Library/LaunchAgents/com.hmax.qlib.alpha360-e0-e6-finalize.plist
```

它每 1800 秒运行一次短命检查，优先级 `Nice=10`、低优先级 I/O；远程未完成时
数秒内退出，验证后 `last exit code=0`，没有常驻 Python。联合 Test 完成后，它会
调用一次性 finalizer，生成主板/all 两套严格回测和报告，然后通过 `launchctl
bootout` 自动卸载自己。

日志：

```text
.qlibAssistant/analysis/alpha360_e0_e6_260828_launchd.log
.qlibAssistant/analysis/alpha360_e0_e6_260828_launchd_stdout.log
.qlibAssistant/analysis/alpha360_e0_e6_260828_launchd_stderr.log
```

在不改变 Selection 排序键的前提下，最终 Selection/Test summary 还补充：

- 方向准确率；
- 50%/80%/95% 中央区间覆盖率；多模型 mixture 使用总方差的 moment-matched
  区间，并在报告中明确说明，不冒充精确 mixture quantile；
- Top1/3/5/10 的日均收益、无摩擦累计、每日等权组合胜率、入选个股胜率；
- 这些是预测诊断，不等于可交易收益；最终以含成交限制、整数手、佣金和滑点的
  stateful account simulation 为准。

新增提交：

```text
895c153  schedule self-unloading alpha360 finalizer
a828162  fix launchd finalizer exit status
6b3c179  report alpha360 calibration and topk diagnostics
a43472e  verify extended alpha360 report metrics
```

## 12. E0～E6 逐轮训练曲线

最终器不再只保留模型最终指标，还会通过 SSH/SCP 拉取七个实验各自的
`epoch_metrics.csv`，并调用：

```bash
python script/report_alpha360_training_curves.py \
  --metrics E0_joint_three_leg=/path/to/E0.csv \
  --metrics E1_shared_four_head=/path/to/E1.csv \
  ... \
  --metrics E6_a_us_four_head=/path/to/E6.csv \
  --expected-epochs 50 \
  --output /path/to/training_curves
```

报告包含：

- 训练 NLL 与 Valid NLL；
- `open1_close2`、`close1_open2`、`open1_open2`、`close1_close2` 四路
  Valid Rank IC；
- 从 warmup 到 cosine 衰减至 `1e-6` 的逐轮学习率；
- 每轮耗时、总训练耗时、最佳 Valid NLL 所在 epoch；
- 每个 horizon 最佳 Valid Rank IC 及对应 epoch。

脚本会强制每个实验恰好包含连续的 `1..50` 轮；缺轮、重复轮次或关键字段出现
非有限值都会使自动收尾失败并保留失败 staging 目录，而不会发布看似完整的报告。
这些曲线只读取 Train/Valid 历史，既不使用 Selection-valid 选交易规则，也不会
提前访问 Test。

相关提交：

```text
5c405de  report alpha360 epoch training curves
```

## 13. Selection 标签边界修复与真实 batch 基准

复核候选预测时发现，旧 E0 Selection 文件有 100 个信号日（截至
`2026-02-13`），而严格的 E1～E6 只有 98 个信号日（截至
`2026-02-11`）。春节休市使 `2026-02-12/13` 信号对应的 T+2 标签跨过
`2026-02-17` 的 Test 起点；如果让 E0 单模型在自己的 100 日上计分，会同时造成
候选比较区间不一致和标签边界泄漏。

修复后协议冻结为：

- Selection 计分：`2025-09-17`～`2026-02-11`；
- 98 个交易日、98,000 个 `(datetime, instrument)` 键；
- canonical key SHA256：
  `50803d9c3c1a56c979766be1a781f4e9e9c712885681857558923cf15473ab9e`；
- 所有候选必须在该区间拥有完全相同的键；
- E0 多出的两日只写入审计字段，不参与单模型排序、组合排序或最终 Selection
  输出；
- Test 仍未读取。

共同 98 日上 E0/E1 的 Selection-valid Rank IC 对照：

| Horizon | E0 Rank IC | E1 Rank IC |
|---|---:|---:|
| `open1_close2` | 0.0499 | 0.0338 |
| `close1_open2` | 0.1021 | 0.1077 |
| `open1_open2` | 0.0410 | 0.0510 |
| `close1_close2` | 0.0450 | 0.0179 |

训练实现没有梯度累积，也没有梯度裁剪。不同日期股票数通过真实 padded date
batch 解决：张量形状为 `[B, N_max, ...]`，补齐位置由 boolean mask 从 Attention
和 NLL 中排除，每个 batch 立即执行一次 optimizer step。

RTX 3060 上单 horizon、64 个训练日的 date batch 基准：

| Date batch | 峰值显存 | 估算秒/epoch | 估算50轮 |
|---:|---:|---:|---:|
| 4 | 1.63 GB | 71.6 | 1.00 h |
| 8 | 3.22 GB | 64.8 | 0.90 h |
| 16 | 6.41 GB | 63.3 | 0.88 h |

大 batch 从 4 增到 16 只带来约 12% 的 epoch 加速，却把每轮 optimizer step 数量
降到四分之一。为保持 E0～E6 优化条件可比，本轮继续使用 `date_batch_size=4`；
后续把 batch size 作为独立超参实验，不和结构实验混在一起。

相关提交：

```text
d166d9a  fix alpha360 selection boundary scoring
```

## 14. E6 UTC as-of 与严格账户回测加固

在 E6 尚未开始训练、Test 仍锁定时完成了并行代码审计。跨市场输入现在不再只按
“美股日历日期早于A股日期”判断，而是显式构造并冻结：

- A股信号时刻：`15:00 Asia/Shanghai`；
- 美股收盘时刻：`16:00 America/New_York`；
- 两者统一转换到 UTC 比较，美股 close 必须严格早于中国信号时刻；
- 时区数据库自动处理美国夏令时；
- 每个 part 同时保存 `a_signal_times_utc` 和 `us_close_times_utc`，并纳入 SHA256；
- padding 股票不再进入市场时间 Transformer，market embedding 和 projection 后再次
  mask，避免 padding token 被 bias 重新变成非零；
- A/US 继续使用完全独立的时间编码器，只有之后的跨市场 Attention 共享；
- 最后一个不足额 date batch 不再人为缩小 loss；仍无梯度累积、无梯度裁剪；
- 第50轮实际使用 cosine 的 `eta_min=1e-6`。

旧 schema 1 store 没有删除，归档为：

```text
E:\qlibAssistant\.qlibAssistant\remote_runs\alpha360_e6_full_260828\
store_schema1_archive_20260828_0345
```

新 schema 2 store 已完成并验证：

| 项目 | 数值 |
|---|---:|
| A股字典 | 2744 |
| 美股字典 | 501 |
| Train/Valid/Selection/Test 日期 | 2402 / 105 / 100 / 99 |
| A股日期总数 | 2706 |
| 美股截面总行数 | 1,312,437 |
| manifest SHA256 | `2af99158114e891e657c8c74259e1ad63416024a189eed8c1c47d026e5237094` |

严格账户回测同时修复：

- fallback 从完整排名继续找下一只，不再错误截断在 Top50；
- fallback 与买不到留现金现在分别生成四套回测和两份报告；
- 跌停无法卖出后，按后续每个 open/close 事件依次重试；
- 滑点成交价不能越过交易所涨跌停价；
- 每日盯市净值不再只在信号日记录；
- 新增 Sharpe、双边换手、敞口、单票集中度、基准回撤和基准 Sharpe；
- 科创/创业板版本明确只剔除 `SH68` 和 `SZ30`；
- 本轮交易规则只用 Selection-valid 冻结并在打开 Test 前写入不可变 manifest；
  回测程序虽然支持可选的 Valid+Selection 稳健规则，但当前流水线没有生成 Valid
  集成预测，因此不能声称本轮已经执行该口径；
- Mac 收尾器下载后再次校验 Selection freeze、Test audit 和所有哈希引用。

新增回归覆盖了“前55只涨停、必须买第56只”的完整 fallback 场景。Alpha360
相关测试共132项通过。

相关提交：

```text
fdf426d  harden alpha360 cross-market asof pipeline
7ad0d17  harden alpha360 strict execution backtests
848241c  pin alpha360 e6 utc-aligned store
```

## 15. E2 完成、学习曲线审计与远端看门狗

04:09，`E2_single_open1_close2` 按协议完成全部50轮，并通过 Selection 候选
认证；随后 `E3_single_close1_open2` 自动接棒。此时 E0、E1、E2 均保持
`test_read=false`，E6 仍处于 `waiting_for_e0_e5_selection`。

E2 的 Train NLL 持续下降，但 Valid 最低 NLL 出现在第19轮，最高 Valid Rank IC
出现在第23轮，说明约第19～23轮后已经出现明显过拟合。完整训练50轮的目的不是
强制使用最后一轮，而是保留完整曲线；候选预测使用独立保存的第23轮 Rank IC
checkpoint：

| 项目 | 数值 |
|---|---:|
| Valid 最佳 epoch | 23 |
| Valid Rank IC | 0.04168 |
| Selection 安全交易日 | 98 |
| Selection Rank IC | 0.03676 |
| Selection Rank ICIR | 0.28000 |
| Selection NLL（缩放目标） | 2.57875 |

截至 E2 完成，E0/E1 的学习曲线审计没有发现标签错位或跨 split 泄漏：未来价格
字段仍是 `O1/C1/O2/C2`，label 是未经截面标准化的对数收益，特征标准化参数只在
Train 拟合，Selection 最后两个无法安全获得 T+2 标签的信号日不参与计分。

为了避免 Windows 任务因休眠、注销或异常退出后静默停住，Mac 收尾器新增了严格
白名单看门狗。每30分钟只检查以下两个已授权任务：

```text
Qlib_Alpha360_Probabilistic_Matrix_260828
Qlib_Alpha360_E6_Combined_260828
```

只有三个条件同时成立才允许执行一次 `schtasks /Run`：流水线是已识别的正常未完成
状态、Scheduled Task 不在 Running、对应路径没有匹配的 Python 进程。状态缺失、
SSH离线、进程查询失败、未知状态或明确 `failed` 都会 fail closed，不会自动重启；
实现中没有任何 kill/taskkill/Stop-Process。执行前还会立即二次检查，所有判断写入：

```text
.qlibAssistant/analysis/alpha360_e0_e6_260828_watchdog.jsonl
```

真实在线检查识别到 E3 PID 和两个 Running 任务，因此正确地没有执行重启。相关
Alpha360 测试146项通过。

相关提交：

```text
a95e417  add strict alpha360 remote task watchdog
```

## 16. Frozen Fixed 外部历史参考与 E3 中期状态

为了回答新模型是否相对当前 Frozen Fixed 有实质改善，最终报告新增一个完全独立的
历史参考。它不参与模型 Selection、交易规则 Selection 或 Test 门禁，只在新模型
Test 已经完成后读取。可比口径固定为：

- `close1_close2`，即 T+1尾盘买入、T+2尾盘卖出；
- CSI1000主板、Top1、10万元账户；
- 佣金万2.35、每笔最低5元、单边5bp滑点；
- 信号区间 `2026-02-24`～`2026-07-17`，97个交易日；
- 历史源文件 SHA256：
  `32db848d16978ff6a3f275931c22d85bd86790ba4510ed1044436d4fd30b67c0`。

Frozen Fixed 既有严格结果：

| 执行口径 | 净累计 | 最大回撤 | 胜率 | 完成交易 |
|---|---:|---:|---:|---:|
| 买不到留现金，5bp | -1.02% | -20.56% | 44.64% | 56 |
| 向后替补，5bp | -38.63% | -47.50% | 41.25% | 80 |

留现金版本是最强同定义参考。替补版本只能作近似比较，因为旧 Fixed 最多寻找第20名，
新严格引擎会遍历完整排名。最终每份报告新增
`fixed_reference_comparison.csv`，比较 Alpha360 与 Fixed 的净累计、最大回撤、
胜率、完成交易数及差值；若源文件 SHA、8行参数网格或信号日数量变化，报告原子失败。

04:42 时 E3 已训练到第33/50轮。前20轮最高 Valid Rank IC 为第16轮
`0.10556`，没有 NaN/Inf，Test 仍未读取。

相关提交：

```text
b23bf0a  add fixed reference to alpha360 final report
```

## 17. E3 完成并自动衔接 E4

04:59，`E3_single_close1_open2` 按统一配置完成全50轮：真实交易日
batch=4，无梯度裁剪、无梯度累积、无早停。流水线在读取 Test 之前将
Valid 最优 checkpoint 物化为 Selection-valid 候选，并且生成了带 SHA256
的候选 manifest：

| 项目 | 数值 |
|---|---:|
| Valid 最佳 epoch | 35 |
| Valid Rank IC | 0.11679 |
| Selection 安全交易日 | 98 |
| Selection Rank IC | 0.10450 |
| Selection Rank ICIR | 0.56311 |
| Selection NLL（缩放目标） | 1.47030 |
| checkpoint SHA256 | `85e926c65372aa60378dfac3bcb8bb39fe91c12a94fd9c32098d9e2725126b6a` |
| candidate manifest SHA256 | `57d1bb319e1193f4d00a3eec034dd5f1b38b4c78287269990415c8fd9ddfd058` |

E3 结束时的门禁状态是 `selection_ready` 且 `test_read=false`。紧接着
`E4_single_open1_open2` 已由 Windows 流水线自动启动，没有人工重跑或同时
启动第二个 GPU 训练进程。本地 Alpha360 全套防泄漏、跨市场、收尾器、
报告和严格回测回归测试重新执行后为 `159 passed`。

## 18. 独立审计发现与严格回测二次修复

在 E4 训练期间，使用只读子 agent 对锁箱和回测链路做了一次独立审计。
审计确认 Selection 先于 Test、只有入选候选会物化 Test，但发现了一个
严重的账户记账问题：买入后的剩余现金同时保存在 `Slot.cash` 和
`Position.cash`，持仓期盯市时等于重复计入一次现金。这不一定改变全部
平仓后的终值，但会严重扭曲最大回撤、Sharpe、敞口、集中度和净值图，
并可能影响交易规则的并列排序。

修复后只保留 `Slot.cash` 作为未投资现金的唯一权威来源，并新增了
“平价行情资金守恒”回归测试：买入后净值必须等于初始资金减买入佣金，
平价卖出后必须再减卖出佣金，持仓期不得出现接近2倍资金的伪净值。
同时增加：

- 利用 Tushare `vol=0` 识别停牌，买入和延迟卖出都不允许成交；
- Selection/Test 回测若还有未平仓头寸则 fail closed，报告也会拒绝加载；
- OHLC、精确涨跌停价和指数缓存的 SHA256、大小、行数和日期范围写入
  pre-Test 交易规则 manifest；
- 报告表述改为“Test 只在模型与交易规则冻结后打开”。后续确定性聚合、
  回测和报告会重读同一冻结 Test 产物，但任何 Test 指标都不回馈到模型、
  阈值或交易规则选择。

最终 Mac 证据包也改为自包含审计结构：`evidence/` 内保存冻结协议、
E0～E6 候选 manifest、训练 configuration、A股/跨市场数据 manifest、所有
最终入选 horizon 的 checkpoint 以及每个入选候选的 Test completion audit。
复制时逐个对照 Selection 冻结中的 SHA256，最后生成
`evidence/evidence_index.json`；任何缺失、哈希不符或 Test horizon 与冻结不一致
都会使 finalizer 原子失败。

## 19. E4 完成并自动衔接 E5

05:49，`E4_single_open1_open2` 按统一协议完成全部50轮。它是只预测
T+1开盘至T+2开盘收益的完整独立模型，不与其他 horizon 共享时间编码器、
股票身份 embedding、截面 Transformer 或输出头。训练始终使用真实交易日
`date_batch_size=4`，没有梯度累积、梯度裁剪或早停。

| 项目 | 数值 |
|---|---:|
| Valid 最佳 epoch | 31 |
| Valid Rank IC | 0.07751 |
| Selection 安全交易日 | 98 |
| Selection Rank IC | 0.04065 |
| Selection Rank ICIR | 0.27064 |
| Selection NLL（缩放目标） | 2.32802 |
| checkpoint SHA256 | `3d8318f27b9ce2710fc326bf017e73852bf4a72dffd9e2dd499bc75fe1618331` |
| candidate manifest SHA256 | `623b96d66f127d8d3fcb71276729ab4c0378098077fb1422778b40b1c163baf1` |

E4 完成后的状态为 `selection_ready`、`test_read=false`。流水线随后自动启动
`E5_single_close1_close2`；E5 预测 T+1尾盘至T+2尾盘收益，与旧 Fixed 的标签
区间一致，但模型结构和训练协议属于本轮 Alpha360 概率实验。

## 20. E6/E0 Test 门禁独立审计

E5 训练期间再次进行只读独立审计。UTC/DST、A/US 双 mask、50轮训练约束以及
E6 checkpoint 到 Test 的对应关系通过；审计同时发现 E0 的首次 Test 入口原本只做
E0 局部认证。总聚合最终虽然仍会拒绝被篡改的冻结，但极端情况下可能先打开 E0
Test，再在后续聚合时发现完整冻结无效，这不符合严格盲测要求。

修复后 E0 在构造 Test `DateStore`、校验 Test part 哈希或 mmap Test 数组之前，必须
先调用与 E1～E6 相同的完整冻结认证，逐项验证：

- protocol 路径与 SHA256；
- 七个候选与预注册实验矩阵完全一致；
- 每个候选 configuration、data manifest、Selection 预测及其 SHA256；
- 每个 horizon 的入选组件及对应 checkpoint SHA256；
- E0 的 source run、数据路径和 Selection 阶段 provenance。

Windows 联合流水线也在任何候选 Test 物化前新增一次独立
`validate-selection`，从而把“候选 Test 尚不存在”与“联合冻结认证失败”分开；后者
立即终止，不能进入 Test。针对真实完整 manifest 的回归测试已覆盖 E0 只入选部分
horizon、E0 完全未入选、checkpoint 被改写及已有 Test 产物冲突，Alpha360 专项
测试共 `160 passed`。

审计还确认 E6 的 501 只美股来自当前 S&P 500 近似池，存在 universe-level
幸存者偏差。它不等于直接使用未来价格，但历史日期已经隐含知道哪些公司后来仍在
指数中。因此本轮 E6 必须标记为“带当前成分股幸存者偏差的探索性跨市场实验”，
不能宣称是严格 point-in-time、无偏的美股 OOS 结论。要消除该问题需要历史时点
成分表与退市股票数据，并重训 E6；本轮不会在 Test 前临时改变数据池。
